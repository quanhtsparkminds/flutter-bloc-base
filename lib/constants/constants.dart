class Constants {
  Constants._();
  static const int passcodeLength = 4;
  static const Duration passcodeExpirationTime = Duration.zero;
  static const Duration animationDuration = Duration(milliseconds: 350);
  static const int timeout = 30;
  static const optTimeOut = 180;
  static const fileRegiterSize = 5;
}
